#notes:

app configuration console commands

```
Options:
      --help      Show help                                            [boolean]
      --version   Show version number                                  [boolean]
  -m, --multiplo  base de tabla de multiplicar               [number] [required]
  -h, --hasta     limite de tablas de multiplicar            [number] [required]
  -l, --list      lista tabla de multiplicar                           [boolean]
```
